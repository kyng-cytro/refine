---
"@refine/desktop": minor
---

Add a global shortcut to cycle through tones (default Ctrl/⌘+Shift+T), showing the newly selected tone in the overlay. The shortcut layer now supports multiple named shortcuts, and settings groups them under a dedicated "Shortcuts" section (Refine selection, Cycle tone) separate from "Behavior".
