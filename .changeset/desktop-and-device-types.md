---
"@refine/desktop": minor
"@refine/api": minor
"@refine/admin": minor
"@refine/schemas": minor
"@refine/db": minor
---

Add the Electron desktop app (Material 3 UI, global-shortcut refine flow, system tray, overlay indicator, `refine://` deep-link pairing) and first-class device types (`mobile` | `desktop` | `browser`) threaded through pairing tokens, sessions, the deep link, and the admin UI.
