{
  "private": true,
  "name": "refine",
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev --filter=@refine/api",
    "admin:dev": "turbo dev --filter=@refine/admin",
    "admin:build": "turbo build --filter=@refine/admin",
    "build": "turbo build",
    "type-check": "turbo type-check",
    "db:generate": "turbo db:generate --filter=@refine/db",
    "db:migrate": "turbo db:migrate --filter=@refine/db"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.2.19",
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.9.6",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "consola": "^3.4.2",
    "zod": "^4.3.6"
  }
}
